( Set A ) ( FRN-16J0825/024 )

Answers are as below : 

select name , salary from employees 
   order by salary desc;

2. select * from employees
   order by salary desc
   LIMIT 5;

3. select * from employees
   Limit 5 Offest 3;

4. select deptName , count(*) from employees
   group by deptName;

5. select deptName, avg(salary) from employees
   group by deptname 
   Having avg(salary) > 50000;

6. select * from product 
   where Pname like '%laptop%';

7. select substring( student_name , 1, 3) from students;

8. select * from students
   where student_name like 'A%';

10. select category, sum(sales_amount) , avg(sales_amount) from Sales
    group by category
    having sum(sales_amount) > 100000;

11. a) The Total Number of employees

12. b) 8

13. c) Having

14. b) Returns all rows from the left table and matching rows from the right table

15. b)HELLO WORLD

16. select c.region, sum(o.total_amount) from Customers c
     inner join Orders o
     on c.customer_id = o.customer_id
     group by c.region
     having sum(o.total_amount) > 10000;

 